<%- include("../../../views/admin/header"); %>

<div class="content-body">

    <div class="container-fluid mt-3">
        <div class="row">

            
      <div class="col-lg-12 col-xl-12">
        <div class="card">
            <div class="card-body">
                <div class="active-member">
                    <div class="table-responsive">
                    <div class="pull-middle col-md-12">
                        <h3>Nouvelle opération</h3>
                    </div>
                    <div class="row">
                    <div class="col-lg-12">
                        <div class="card bg-light mb-3">
                            <div class="card-header"><h4>Veuillez renseigner les champs</h4> </div>
                            <div class="card-body">
                        
                                <% data.forEach(function (profile) { %>
                         <form action="/profile/update/<%= profile.id_profile %>?_method=PUT " method="POST">
                            
                                <div class="form-group row">
                                    <label class="col-sm-2 col-form-label">Nom profile</label>
                                    <div class="col-sm-10">
                                      <input  type="text" class="form-control" name="nom" value="<%= profile.nom %>" required>
                                    </div><br><br>

                                <div class="col-md-12"">
                                    <div ><span>Privilèges: </span> <span><small>(The colored ones are the privileges accorded from the begining.)</small></span> </p>
                                  </div>
                                  <% data1.forEach(function (operation) { %>
                                        <% if(data2.find( name => name.id_op === operation.id_op)) { %>  
                                  <div class="form-group col-md-12" style="background-color: aquamarine;">
                                      <input type="checkbox"  id="<%= operation.id_op %>" name="operation" value="<%= operation.op_nom %>" >
                                      <label for=""> <%= operation.op_nom %> </label><br>
                                  </div>
                                  <%  } else{ %>
                                    <div class="form-group col-md-12">
                                        <input type="checkbox"  id="<%= operation.id_op %>" name="operation" value="<%= operation.op_nom %>" >
                                        <label for=""> <%= operation.op_nom %> </label><br>
                                    </div>

                                  <% }}) %>
                                 

                                <script language="javascript">
                                    function UpdateConfirm() {
                                               var userPreference;
                                               if (confirm("Do you want to save changes?") == true) {
                                               return true;} 
                                               else {
                                               return false;}
                                
                                               }
                                    </script>
                           
                                  <div class="form-group">
                                    <div class="col-md-6 offset-md-4">
                                        <button type="submit" class="btn btn-primary" onclick="return UpdateConfirm()">
                                          <i class="fa fa-check-circle-o" >
                                            Enregistrer
                                          </i>
                                        </button>
                                        <a href="/profileList" class="btn btn-danger" ><i class="fa fa-ban" > Annuler</i></a>
                                    </div>  </div>
                                

                           
                            </form>
                            <% }) %>
                        </div></div></div></div>
                    

                    </div>

                    </div>
    </div>
    </div></div> </div></div>
    </div> 
    </div>      

    <%- include("../../../views/admin/footer"); %>